script(type='text/javascript').
  var appUrl = window.location.origin;

  if (window.opener) {
      window.opener.focus();

    // if(window.opener.loginCallBack) {
    //   window.opener.loginCallBack();
    // }
  }

  window.opener.location = appUrl + '/polls';
  window.close();
